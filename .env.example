NEXT_PUBLIC_API_URL=http://localhost:8081
# Optional server-side target for a same-origin /api proxy (recommended on Vercel).
# When set, leave NEXT_PUBLIC_API_URL empty in the deployed frontend.
API_PROXY_TARGET=
